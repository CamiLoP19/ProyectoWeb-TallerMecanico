@page "/"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirigir automáticamente al login page (Razor Pages)
        Navigation.NavigateTo("/loginpage", true);
    }
}
